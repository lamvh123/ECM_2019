import {Center} from './center';
import {Building} from './building';

export class Room {
  Building: Building;
  Capacity: number;
  Center: Center;
  Id: number;
  RoomNumber: string;
  // $id: string;
  // $ref: string;
}
